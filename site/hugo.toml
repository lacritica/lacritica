baseurl = "https://www.lacritica.ar/"
languageCode = "en-us"
languageLang = "en"
title = "La Crítica"
theme = "hugo-xmag"

# RSS, categories and tags disabled for an easy start
# See configuration options for more details: 
# https://gohugo.io/getting-started/configuration/#toml-configuration
#disableKinds = ["RSS", "taxonomy", "taxonomyTerm"]

[params]
images = ["img/batalla1.jpeg"]
description = "La Crítica del Derecho"
social_image = "/img/batalla1.jpeg"

# The tiles section is only an on/off switch for the home band, not a page.
# Keep it out of the published site.
[[cascade]]
  [cascade.build]
    render = 'never'
    list = 'local'
    publishResources = false
  [cascade._target]
    path = '/tiles'
